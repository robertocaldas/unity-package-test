using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Monobee : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.LogError("something here........xxx.........");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
